<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sjl.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="com.sjl.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Wed Feb 19 16:56:34 CST 2020.
        -->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="title" jdbcType="VARCHAR" property="title"/>
        <result column="tags" jdbcType="VARCHAR" property="tags"/>
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified"/>
        <result column="comment_count" jdbcType="INTEGER" property="commentCount"/>
        <result column="like_count" jdbcType="INTEGER" property="likeCount"/>
        <result column="view_count" jdbcType="INTEGER" property="viewCount"/>
        <result column="creator" jdbcType="BIGINT" property="creator"/>
    </resultMap>

    <update id="addViewCount" parameterType="com.sjl.community.model.Question">
        update question
        <set>
            <if test="viewCount != null">
                view_count = view_count + #{viewCount,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>
    <update id="addCommentCount" parameterType="com.sjl.community.model.Question">
        update question
        <set>
            <if test="viewCount != null">
                comment_count = comment_count + #{commentCount,jdbcType=INTEGER},
            </if>
        </set>
        where id = #{id,jdbcType=BIGINT}
    </update>

    <select id="findByTagsREGEXP" parameterType="com.sjl.community.model.Question" resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="id != null">
                and id != #{id}
            </if>
            <if test="tags != null and tags != ''">
                and tags regexp #{tags}
            </if>
        </where>
        order by gmt_create desc limit 20
    </select>

    <select id="countByCondition" parameterType="com.sjl.community.dto.QuestionQueryDto" resultType="Integer">
        select count(*) from question
        <where>
            <if test="creatorId != null and creatorId != ''">
                and creator = #{creatorId}
            </if>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tags regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
    </select>

    <select id="findByCondition" parameterType="com.sjl.community.dto.QuestionQueryDto" resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="creatorId != null">
                and creator = #{creatorId}
            </if>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tags regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no_comment'">
                and comment_count = 0
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
        order by top desc,
        <if test="sort != null and sort != '' and (sort == 'hot' || sort == 'hot7' || sort == 'hot30')">
            comment_count desc,
        </if>
        gmt_create desc
        limit #{offerIndex}, #{pageSize}
    </select>
</mapper>